# Copy to .env and fill in. All VITE_ vars are embedded into the client bundle.

# Google Analytics 4 Measurement ID (leave empty to disable analytics + cookie banner).
VITE_GA_ID=

# Ko-fi handle, used to build https://ko-fi.com/<handle> (leave empty to hide the donate link).
VITE_KOFI_HANDLE=
